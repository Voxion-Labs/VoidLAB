"use client";

import GitHubPanel from "@/components/editor/GitHubPanel";

export default function GitHubWorkspacePanel() {
  // GitHubPanel is now fully self-contained — no props needed.
  return <GitHubPanel />;
}
